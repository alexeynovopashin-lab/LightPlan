# Spikes

Throwaway prototypes: iteration 3 (timebar) and iteration 4 (map canvas). Each
spike is its own project and is never linked into the app. The outcome is
numbers and pictures recorded in `light_plan:Light_Plan/DECISIONS.md`, not code
that moves into `Packages/`.
